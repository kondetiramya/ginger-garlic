name: CIPipeline2

on:
  pull_request:
    branches: [ main ]
env:
  IMAGE_NAME: ginger-garlic
  GHCR_ROOT: ghcr.io
  GHCR: ghcr.io/kondetiramya

jobs:
  CIPipeline:
    uses: kondetiramya/ginger-garlic/.github/workflows/ci.yml@main
